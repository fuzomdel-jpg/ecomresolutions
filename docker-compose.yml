services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ecom
      POSTGRES_PASSWORD: ecomdev
      POSTGRES_DB: ecomresolutions
    ports:
      - "5432:5432"
    volumes:
      - ecom_pg:/var/lib/postgresql/data

volumes:
  ecom_pg:
